// html2canvas loader - imports the library locally
import html2canvas from 'html2canvas';

// Make html2canvas available globally for components that expect it
window.html2canvas = html2canvas;

export default html2canvas;