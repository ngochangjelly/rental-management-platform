{
  "name": "rental-management-platform",
  "version": "1.0.0",
  "description": "AI-powered rental management platform frontend - SPA client",
  "main": "public/index.html",
  "scripts": {
    "prestart": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true",
    "start": "npx serve public -l 3000",
    "predev": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true",
    "dev": "npx serve public -l 3000",
    "build": "echo 'SPA build completed - static files ready'",
    "preview": "npx serve public",
    "lint": "eslint public/js --ext .js",
    "lint:fix": "eslint public/js --ext .js --fix"
  },
  "keywords": [
    "rental",
    "tenancy",
    "agreement",
    "ai",
    "claude",
    "property",
    "management"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {},
  "optionalDependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "serve": "^14.2.0"
  }
}